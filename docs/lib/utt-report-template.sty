\newcommand{\frontpage}{\include{lib/frontpage}}

\def \semester#1{\gdef\@semester{#1}}
\def \branch#1{\gdef\@branch{#1}}
\long\def \abstracttext#1{\gdef\@abstracttext{#1}}
\def \schooltutor#1{\gdef\@schooltutor{#1}}

\def \companyname#1{\gdef\@companyname{#1}}
\def \companyplace#1{\gdef\@companyplace{#1}}
\def \companytutor#1{\gdef\@companytutor{#1}}
\def \companylogo#1{\gdef\@companylogo{#1}}

\def \keywordone#1{\gdef\@keywordone{#1}}
\def \keywordtwo#1{\gdef\@keywordtwo{#1}}
\def \keywordthree#1{\gdef\@keywordthree{#1}}
\def \keywordfour#1{\gdef\@keywordfour{#1}}

% Geometry
\usepackage[a4paper, top=2cm, bottom=3cm, left=1.5cm, right=1.5cm]{geometry}

\usepackage{array}

\usepackage{wrapfig}

\usepackage{booktabs}       % professional-quality tables

%\renewcommand{\familydefault}{\sfdefault}

% Fonts
%\usepackage{lmodern}
%\renewcommand{\familydefault}{\sfdefault}

% Arial
\usepackage{fontspec}
\setmainfont{Arial}
%\setsansfont[
%BoldFont=lib/ArialUnicode.ttf,
%ItalicFont=lib/ArialUnicode.ttf,
%BoldItalicFont=lib/ArialUnicode.ttf
%]{lib/ArialUnicode.ttf}

\setlength{\parskip}{0cm plus5mm minus2mm}

\usepackage{hyperref}
\usepackage[official]{eurosym}

\usepackage{subfig}

%\usepackage{natbib}

% Outline text
\usepackage{fancybox}

% Text pos to position texts in absolute coords for the first page.
\usepackage[absolute,overlay]{textpos}
\usepackage{nameref}

% Table with adjustable sizes.
\usepackage{tabularx, makecell}

% Custom colors
\usepackage{xcolor}
    \definecolor{lightgray}{rgb}{.9,.9,.9}
    \definecolor{darkgray}{rgb}{.2,.2,.2}
    \definecolor{pniblue}{RGB}{62, 123, 179}
    \definecolor{white}{rgb}{1, 1, 1}
    
\usepackage{titlesec}
\titleformat*{\section}{\Large\bfseries\color{pniblue}}
\usepackage{titlesec}
\titleformat*{\subsection}{\bfseries\color{darkgray}}

% Assets path
\usepackage{graphicx}
    \graphicspath{{./assets/}}

\usepackage{floatrow}

% Maths
\usepackage{amsmath}

\usepackage{tikz}
\usetikzlibrary{decorations.pathmorphing}
\usetikzlibrary{shapes}

\usepackage{multicol}

% Display code
\usepackage{listings}
    \definecolor{dkgreen}{rgb}{0,0.6,0}
    \definecolor{gray}{rgb}{0.5,0.5,0.5}
    \definecolor{mallow}{rgb}{0.58,0,0.82}

    % Captions style
    \renewcommand{\lstlistingname}{Source}

    % Listing list style
    \renewcommand\lstlistlistingname{Fichiers source}

    % Custom syntax highlighting
    \lstdefinelanguage{JavaScript}{
      keywords={typeof, new, true, false, catch, function, return, null, catch, switch, var, if, in, while, do, else, case, break class, let, const},
      ndkeywords={class, export, boolean, throw, implements, import, this},
      basicstyle=\ttfamily\scriptsize,
      sensitive=false,
      comment=[l]{//},
      morecomment=[s]{/*}{*/},
      morestring=[b]',
      morestring=[b]""
    }

    \lstset{
        language=JavaScript,
        aboveskip=3mm,
        belowskip=3mm,
        showstringspaces=false,
        columns=flexible,
        basicstyle={\small\ttfamily},
        numbers=none,
        numberstyle=\tiny\color{gray},
        keywordstyle=\color{blue},
        commentstyle=\color{dkgreen},
        stringstyle=\color{mallow},
        breaklines=true,
        breakatwhitespace=true,
        tabsize=4,
        frame=single
    }

% Header/footer
\usepackage{fancyhdr}
\renewcommand{\headrulewidth}{0.5pt}% header rule
\renewcommand{\headrule}{\hbox to\headwidth{%
  \color{pniblue}\leaders\hrule height \headrulewidth\hfill}}
    \fancypagestyle{utt}{
        \setlength{\headheight}{23.96pt}
        \pagestyle{fancy}
        \fancyhf{}
        \rhead{\color{pniblue}Version \version}
        \lhead{\color{pniblue}\myshorttitle}
        \fancyfoot[C]{Page \thepage /5}
    }

    \pagestyle{utt}
    
\hyphenation{system}
\hyphenation{neuroscience}
\hyphenation{Factory}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%% Following part is for editing, should be commented out in the final version:
\usepackage{xcolor}
\newcommand{\todo}[1]{{\color{red}{\textbf{ToDo: #1}}}}
\usepackage{lipsum}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%