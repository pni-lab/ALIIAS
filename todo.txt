# build docker
docker build -t sfb289/pseudoid:v0.1 .
docker build -t sfb289/pseudoid:latest .

docker push  sfb289/pseudoid:v0.1
docker push  sfb289/pseudoid:latest

# run it
docker run -d -p 80:80 --mount type=bind,source="$(pwd)"/dev_key,target=/key pseudoid

# check if it runs
docker ps

# simply type this into the browser:
# 127.0.0.1/pseudoID/generate

# docker-machine ip default to check the localhost address

# pyinstaller -F -p pseudoID/ --add-data "pseudoID/templates:pseudoID/templates" --add-data "pseudoID/static:pseudoID/static" scripts/start_pseudoid.py

# for windows:
# pyinstaller -F -p pseudoID\ --add-data "pseudoID\templates;pseudoID\templates" --add-data "pseudoID\static;pseudoID\static" scripts\start_pseudoid.py


# pyinstaller